$base: ../phblog_advanced.yaml
experiment_name: phblog_advanced_glm

coord:
  volumes:
    - "./phblog_advanced_glm.sqlite3:/app/results.sqlite3"

eval:
  command: ["AdvancedWebTesting", "--log=remote_logger"]
  environment:
    llm.model: z-ai/glm-4.6
    llm.provider: z-ai
